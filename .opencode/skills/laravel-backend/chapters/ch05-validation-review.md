# Validation & Review

## Load When
final backend review before handoff.

Check DTO/generated diff, routes, controller thinness, services, transactions, auth/scope, side effects, events/jobs, tests. Run targeted tests or `php artisan test --stop-on-failure` when feasible.
