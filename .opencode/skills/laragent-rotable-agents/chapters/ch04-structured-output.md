# Structured Output

## Load When
designing final JSON response contracts.

`$responseSchema` constrains final model response after tool loop. Tool result remains PHP-controlled evidence. Do not ask the model to invent IDs, counts, URLs, or DB state not returned by the tool.
