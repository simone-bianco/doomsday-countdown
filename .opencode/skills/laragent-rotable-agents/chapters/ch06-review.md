# Review

## Load When
reviewing an agent implementation.

Check namespace, provider, rotable base, key injection timing, tool annotations, forced tool name, write guard, deterministic service, schema, command evidence, and no-key failure behavior.
