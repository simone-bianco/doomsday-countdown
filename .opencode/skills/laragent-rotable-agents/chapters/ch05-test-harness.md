# Test Harness

## Load When
validating agents without live LLM calls.

Default command path should be TOOL_ONLY: verify `#[Tool]` by reflection and call the shared runner. `--llm` is explicit and should fail clearly on missing rotable keys.
