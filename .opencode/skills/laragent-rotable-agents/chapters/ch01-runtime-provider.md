# Runtime Provider

## Load When
configuring LarAgent providers, drivers, base URLs, and namespaces.

Inspect `config/laragent.php`, current agent class, and provider driver. Prefer `OpenAiCompatible` when `OPENAI_BASE_URL` matters. Provider config is wiring; it is not proof that a rotable key exists.
