# Cheatsheet

```bash
php -d memory_limit=1G artisan typescript:transform
php -d memory_limit=1G artisan form-bridge:generate
```

Never: controller validation arrays, raw mutation `axios/fetch`, business logic in controller.
