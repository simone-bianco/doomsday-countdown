# Styling & Icons

        ## Load When
        styling components and binding icons.

        - Use `cn()` for class merging.
- Use `ui-*` CSS variables.
- Use `defineModel` for v-model.
- Icons come from `lucide-vue-next` as component bindings, not lowercase strings.
- Expose `ui?: Partial<ComponentUI>` for deep class injection.
