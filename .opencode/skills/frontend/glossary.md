# Glossary

**Base component** — reusable package atom/molecule.
**Domain component** — app-specific component under `resources/js/Components`.
**Page orchestrator** — Inertia page that composes, not implements heavy UI.
**Stale response guard** — Ignore async payloads no longer matching active state.
