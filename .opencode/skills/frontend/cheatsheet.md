# Cheatsheet

- No native primitives when package components exist.
- No raw mutation `axios/fetch`.
- Use `cn()` for class attributes.
- Use lucide component icons.
- Use `defineModel`.
- Pages target small size; split components early.
